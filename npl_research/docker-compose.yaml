version: '3.9'

services:
  chromadb:
    image: chromadb/chroma:latest
    volumes:
      - ./chroma_data:/data  # 將資料存儲在本地目錄
    environment:
      - IS_PERSISTENT=TRUE
      - PERSIST_DIRECTORY=/data
    ports:
      - "8000:8000"